{
  "name": "@outcome/shared",
  "version": "1.0.0",
  "description": "Shared governance reason codes and types for Platform and Mandate services",
  "main": "dist/governanceReasonCodes.cjs",
  "types": "dist/governanceReasonCodes.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && node scripts/build-cjs.cjs",
    "postinstall": "npm run build",
    "prepublishOnly": "npm run build",
    "setup:github": "bash scripts/setup-github-repo.sh",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": [
    "governance",
    "mandate",
    "reason-codes"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
